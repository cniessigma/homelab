apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: foundry
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: foundry
  project: default
  sources:
    - path: k8s/deployments/configs/foundry
      repoURL: git@github.com:cniessigma/homelab.git
      targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
