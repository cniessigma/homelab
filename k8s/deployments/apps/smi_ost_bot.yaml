apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smi_ost_bot
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: smi_ost_bot
  project: default
  sources:
    - path: k8s/deployments/configs/smi_ost_bot
      repoURL: git@github.com:cniessigma/homelab.git
      targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
