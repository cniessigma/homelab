apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: obojima-tools
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: obojima-tools
  project: default
  sources:
    - path: k8s/deployments/configs/obojima-tools
      repoURL: git@github.com:cniessigma/homelab.git
      targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
