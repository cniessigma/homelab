apiVersion: v1
kind: Pod
metadata:
  name: busybox-jumpbox
  labels:
    app: busybox-jumpbox
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command: ["/bin/sh", "-c", "sleep infinity"]
      stdin: true
      tty: true
  restartPolicy: Always
